dump
*.pyc
*.swp
*.swo
*.pem
.DS_Store
full_results/
.ipynb_checkpoints/

# Personal time-tracking log — kept local, not part of the public deliverable.
TIME_LOG.md

# Track results/ (deliverables + slim run outputs), but never the large regenerable
# model artifacts or the quarantined stale/residual runs.
*.pkl
pytorch_model*.bin
*.safetensors
results/data/_residual_quarantine/
pytorch_model.bin.index.json
cache-*.arrow
